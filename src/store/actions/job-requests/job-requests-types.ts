export enum JobRequestsActionTypes {
  GetAllJobs = '[JobRequests] GetAllJobs',
  GetAllJobsSuccess = '[JobRequests] GetAllJobs success',
  GetAllJobsFailure = '[JobRequests] GetAllJobs failure',

  RespondOnCurrentJob = '[JobRequests] Respond on current job',
  RespondOnCurrentJobSuccess = '[JobRequests] Respond on current job success',
  RespondOnCurrentJobFailure = '[JobRequests] Respond on current job failure',
}
