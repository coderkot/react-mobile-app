import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    fontFamily: 'Roboto',
    fontStyle: 'normal',
    fontWeight: 'normal',
    fontSize: 32,
    lineHeight: 37,
    textAlign: 'center',
    color: '#000',
  },
});

export { styles };
