import { StyleSheet } from 'react-native';

export const LaboratoryStyles = StyleSheet.create({
  menuItems: {
    backgroundColor: '#fff',
    marginBottom: 16,
    marginRight: 15,
    marginLeft: 16,
    borderRadius: 8,
  },
  searchBar: {
    marginHorizontal: 16,
    borderRadius: 8,
  },
});
